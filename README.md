# MRI-tumor-Detection-app
A web app that takes an uploaded MRI image, runs it through your trained Keras model, and returns a tumor classification (glioma, meningioma, pituitary, or no tumor) with confidence scores — built as a FastAPI backend serving both the prediction API and a simple upload/results frontend, deployed as a single online app.
